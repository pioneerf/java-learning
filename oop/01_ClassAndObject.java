/**
 * 面向对象基础：类和对象
 *
 * 类是对象的模板/蓝图，对象是类的具体实例。
 * 本示例\n * 类是对象的模板/蓝图，对象是类的具体实例。\n * 本示例通过"学生"这个现实概念来演示类和对象。\n */\n\n// ========== 类的定义 ==========\n/**\n * 学生类\n *\n * 一个类包含：\n * 1. 属性（成员变量/字段）- 描述对象的状态\n * 2. 方法 - 描述对象的行为\n */\nclass Student {\n\n    // ===== 属性（成员变量） =====\n    // 实例变量 - 每个对象都有自己的副本\n    String name;       // 姓名\n    int age;           // 年龄\n    String studentId;  // 学号\n    double score;      // 成绩\n\n    // 静态变量（类变量）- 所有对象共享同一个副本\n    static String schoolName = \"第一中学\";  // 学校名称\n    static int studentCount = 0;            // 学生总数\n\n    // ===== 构造方法 =====\n    // 构造方法在创建对象时自动调用，用于初始化对象\n    // 构造方法名必须与类名相同，没有返回值\n\n    // 无参构造方法\n    public Student() {\n        studentCount++;  // 每创建一个学生，总数+1\n    }\n\n    // 有参构造方法（重载）\n    public Student(String name, int age, String studentId) {\n        this.name = name;          // this 指代当前对象的引用\n        this.age = age;\n        this.studentId = studentId;\n        this.score = 0.0;\n        studentCount++;\n    }\n\n    // 全参构造方法\n    public Student(String name, int age, String studentId, double score) {\n        this.name = name;\n        this.age = age;\n        this.studentId = studentId;\n        this.score = score;\n        studentCount++;\n    }\n\n    // ===== 实例方法 =====\n    // 必须通过对象调用\n\n    // 自我介绍\n    public void introduce() {\n        System.out.println(\"大家好，我叫\" + name + \"，今年\" + age + \"岁，学号是\" + studentId);\n        System.out.println(\"我来自\" + schoolName);  // 实例方法可以访问静态变量\n    }\n\n    // 学习方法\n    public void study(String subject) {\n        System.out.println(name + \"正在学习\" + subject);\n    }\n\n    // 考试方法\n    public void takeExam() {\n        System.out.println(name + \"正在考试...\");\n    }\n\n    // 获取成绩等级\n    public String getGrade() {\n        if (score >= 90) return \"A（优秀）\";\n        if (score >= 80) return \"B（良好）\";\n        if (score >= 70) return \"C（中等）\";\n        if (score >= 60) return \"D（及格）\";\n        return \"F（不及格）\";\n    }\n\n    // ===== 静态方法（类方法） =====\n    // 通过类名直接调用，无需创建对象\n    // 静态方法只能访问静态变量，不能直接访问实例变量\n\n    /** 获取学校名称 */\n    public static String getSchoolName() {\n        return schoolName;\n    }\n\n    /** 获取学生总数 */\n    public static int getStudentCount() {\n        return studentCount;\n    }\n\n    // 修改学校名称\n    public static void setSchoolName(String newName) {\n        schoolName = newName;\n    }\n}\n\n\n/**\n * 主类 - 测试Student类\n */\npublic class ClassAndObject {\n\n    public static void main(String[] args) {\n\n        System.out.println(\"===== 类和对象示例 =====\");\n\n        // ===== 创建对象（实例化） =====\n        System.out.println(\"\\n1. 使用无参构造方法创建对象：\");\n        Student stu1 = new Student();\n        stu1.name = \"小明\";         // 直接访问属性并赋值\n        stu1.age = 15;\n        stu1.studentId = \"2024001\";\n        stu1.score = 85.5;\n\n        // 调用实例方法\n        stu1.introduce();\n        stu1.study(\"数学\");\n        stu1.takeExam();\n        System.out.println(\"成绩等级：\" + stu1.getGrade());\n\n        System.out.println(\"\\n2. 使用有参构造方法创建对象：\");\n        Student stu2 = new Student(\"小红\", 16, \"2024002\");\n        stu2.score = 92.0;  // 单独设置成绩\n        stu2.introduce();\n        System.out.println(\"成绩等级：\" + stu2.getGrade());\n\n        System.out.println(\"\\n3. 使用全参构造方法创建对象：\");\n        Student stu3 = new Student(\"小刚\", 15, \"2024003\", 72.0);\n        stu3.introduce();\n\n        // ===== 静态变量和静态方法的访问 =====\n        System.out.println(\"\\n===== 静态成员访问 =====\");\n        // 通过类名访问静态方法（推荐方式）\n        System.out.println(\"学校名称：\" + Student.getSchoolName());\n        System.out.println(\"学生总数：\" + Student.getStudentCount());\n\n        // 通过对象访问静态方法（不推荐，但可以工作）\n        System.out.println(\"通过对象访问学校名称：\" + stu1.getSchoolName());\n\n        // ===== 静态变量是共享的 =====\n        Student.setSchoolName(\"第一高级中学\");  // 修改学校名\n        System.out.println(\"\\n修改学校名称后：\");\n        stu1.introduce();  // stu1的schoolName也变了\n        stu2.introduce();  // stu2的schoolName也变了\n\n        // ===== 对象引用特性 =====\n        System.out.println(\"\\n===== 对象引用 =====\");\n        Student ref1 = stu1;  // ref1 和 stu1 指向同一个对象\n        ref1.name = \"大明\";   // 通过 ref1 修改属性\n        System.out.println(\"stu1的名字：\" + stu1.name);  // 也变成了\"大明\"\n        System.out.println(\"ref1的名字：\" + ref1.name);  // \"大明\"\n        System.out.println(\"stu1 == ref1：\" + (stu1 == ref1));  // true（同一个引用）\n        System.out.println(\"stu1 == stu2：\" + (stu1 == stu2));  // false（不同的对象）\n    }\n}"}